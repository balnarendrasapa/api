# API

## Installing Fastapi dependencies

```bash
pip install poetry
poetry install --with fastapi
```

## Installing Fastapi dependencies

```bash
pip install poetry
poetry install --with fastapi
```